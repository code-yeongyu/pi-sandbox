# Contributing

Thanks for contributing to `pi-sandbox`.

Run `npm install`, `npm run check`, and `npm test` before opening a pull request. Keep changes focused, strict-TypeScript clean, and aligned with the sandbox architecture documented in `.sisyphus/plans/`.
