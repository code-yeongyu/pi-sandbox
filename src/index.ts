// src/index.ts — Extension factory: pi.registerTool / pi.registerCommand / pi.registerFlag / pi.on(...)
import type { ExtensionAPI } from "./pi/index.js";

export default function piSandboxExtension(_api: ExtensionAPI): void {
	// implemented in Wave 2
}
